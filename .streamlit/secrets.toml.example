# Example secrets file for local development (do NOT commit real secrets)
# You can set these values in Streamlit Community Cloud instead.
# DATABASE_URL = "postgres://user:password@host:5432/dbname"
